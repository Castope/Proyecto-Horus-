# Copia estas variables a backend/.env únicamente para activar la IA.
# Sin ellas, el chatbot consulta y muestra directamente el catálogo publicado.
CHATBOT_AI_ENABLED=false
OPENAI_API_KEY=
# ID de un modelo habilitado en tu cuenta y compatible con Responses API.
CHATBOT_MODEL=
